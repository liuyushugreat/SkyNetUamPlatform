/**
 * Regulator navigation config (placeholder).
 *
 * Should remain regulator-only (no cross imports to other apps).
 */

export type NavItem = {
  label: string;
  path: string;
};

export const regulatorNavigation: NavItem[] = [
  { label: "Regulator", path: "/regulator" },
];


