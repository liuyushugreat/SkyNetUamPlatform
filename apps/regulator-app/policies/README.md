# Regulator Policies (Future)

Reserved for regulator-only policy configuration, such as:
- compliance thresholds
- alerting rules
- audit retention policies

Keep these policies isolated from `citizen-app` and `operator-ops` so the
regulator build can remain minimal and independently deployable.


