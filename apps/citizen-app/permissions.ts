/**
 * Citizen app permissions (placeholder).
 *
 * Today: role selection is done locally in App.tsx.
 * Future: these will be enforced via auth guards.
 */

export const citizenPermissions = {
  roles: ["CITIZEN"],
  scopes: [] as string[],
};


