# SkyRwa artifact -- Python dependencies (reviewer reproduction).
# Tested with Python 3.10+.  For exact pinned versions see
# ../requirements.txt (used by the Dockerfile clean-room build).

# Core data models
pydantic>=2.0.0

# RDF / Knowledge Graph
rdflib>=7.0.0

# SHACL validation
pyshacl>=0.25.0

# rudof SHACL engine (dual-engine comparison, Table 11 / Fig. 2)
pyrudof>=0.3.0

# Cryptographic provenance (Ed25519 signing)
cryptography>=41.0.0

# Visualization (Fig. 2)
matplotlib>=3.8.0
numpy>=1.24.0

# OOPS!/FOOPS! web-service evaluation scripts (optional, network access)
requests>=2.31.0
